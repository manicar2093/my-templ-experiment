package userpages

import "templ-demo/internal/domain/models"
import "templ-demo/cmd/web/ui/layouts"
import "net/http"
import "fmt"

templ EditUserPage(user *models.User) {
    @layouts.Initial() {
        @UserForm(user, UserFormProperties{
            Action: fmt.Sprintf("/users/%s", user.Id) ,
            Method: http.MethodPut,
        })
    }
}
